% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assess_data_code.R
\name{assess_data_code}
\alias{assess_data_code}
\title{Assess the FAIRness of the data and code shared in articles (rtransparent)}
\usage{
assess_data_code(
  x,
  id_col = NULL,
  data_metric_version = "0.8",
  code_metric_version = "0.7_software",
  data_col = "open_data_links",
  code_col = "open_code_links",
  sep = " ; ",
  quiet = FALSE,
  ...
)
}
\arguments{
\item{x}{One of: a data frame from \code{rtransparent::rt_data_code_pmc()} /
\code{rt_all_pmc()} (with \code{open_data_links} / \code{open_code_links} columns); a named
list with those elements; or a character vector of \code{" ; "}-joined data-link
strings.}

\item{id_col}{Optional name of a column in \code{x} identifying the source article
(e.g. \code{"pmid"} or \code{"doi"}); used to label each result.}

\item{data_metric_version}{Metric version for data identifiers (default
\code{"0.8"}).}

\item{code_metric_version}{Metric version for code repositories (default
\code{"0.7_software"}).}

\item{data_col, code_col}{Column/element names holding the joined links
(defaults match rtransparent: \code{"open_data_links"}, \code{"open_code_links"}).}

\item{sep}{Separator rtransparent uses to join identifiers (default \code{" ; "}).}

\item{quiet}{If \code{FALSE} (default), print per-identifier progress.}

\item{...}{Passed to \code{\link[=assess_fair]{assess_fair()}}.}
}
\value{
A data frame with one row per (article, kind, identifier): \code{source}
(article id), \code{kind} (\code{"data"} or \code{"code"}), and the columns of
\code{\link[=assess_fair_batch]{assess_fair_batch()}}. Each unique identifier is assessed once.
}
\description{
Bridges \pkg{rtransparent} and rfair: takes the data/code identifiers
rtransparent extracts from articles (its \code{open_data_links} and
\code{open_code_links} columns) and scores each against the FAIR metrics. Data
identifiers are scored with the FsF data metrics and code repositories with
the FRSM software metrics.
}
\examples{
\donttest{
assess_data_code(list(open_data_links = "https://doi.org/10.5281/zenodo.8347772",
                      open_code_links = "https://github.com/pangaea-data-publisher/fuji"))
}
}
\seealso{
\code{\link[=assess_fair_batch]{assess_fair_batch()}}, \code{\link[=split_identifiers]{split_identifiers()}}, \code{\link[=assess_fair]{assess_fair()}}
}
