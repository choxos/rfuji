% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assess.R
\name{assess_fair}
\alias{assess_fair}
\title{Assess the FAIRness of a research data object.}
\usage{
assess_fair(
  id,
  metric_version = "0.8",
  use_datacite = TRUE,
  metadata_service_endpoint = NULL,
  metadata_service_type = metadata_service_types(),
  test_debug = FALSE,
  resolve = TRUE,
  timeout = 15,
  use_headless = FALSE
)
}
\arguments{
\item{id}{A persistent identifier or URL (DOI, Handle, ARK, URN, ...).}

\item{metric_version}{Metric version to use (see \code{\link[=rfair_metric_versions]{rfair_metric_versions()}}).}

\item{use_datacite}{Whether to query DataCite for registry metadata.}

\item{metadata_service_endpoint}{Optional URL of an additional metadata
document to harvest, or a ready protocol query URL (for example an OAI-PMH
\code{GetRecord} URL, an OGC CSW \code{GetRecordById} URL, a SPARQL query URL, or a
DCAT / schema.org JSON-LD / RO-Crate / DataCite / Crossref / CKAN document).
The response is parsed with the same format-gated collectors used for
content negotiation, so only a recognized metadata document contributes.}

\item{metadata_service_type}{Type hint for \code{metadata_service_endpoint}.
\code{"schema_org"} is harvested as JSON-LD; the others are tried as an XML
metadata document, then RDF.}

\item{test_debug}{If \code{TRUE}, collect debug log messages in the result.}

\item{resolve}{If \code{TRUE}, resolve the identifier to its landing page.}

\item{timeout}{Per-request timeout in seconds.}

\item{use_headless}{If \code{TRUE} and the optional \code{chromote} package is
installed, render JavaScript-heavy landing pages with a headless browser
before harvesting embedded metadata.}
}
\value{
A \link{fair_assessment} object.
}
\description{
Resolves a persistent identifier or URL, harvests its metadata, and scores it
against the FAIRsFAIR metrics, entirely in R.
}
\examples{
\donttest{
a <- assess_fair("https://doi.org/10.5281/zenodo.8347772")
summary(a)
}
}
