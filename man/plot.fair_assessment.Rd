% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.fair_assessment}
\alias{plot.fair_assessment}
\title{Plot a FAIR assessment as a scorecard}
\usage{
\method{plot}{fair_assessment}(
  x,
  type = c("category", "metric", "sunburst"),
  colors = .fair_cat_colors,
  show_maturity = (match.arg(type) == "category"),
  main = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{fair_assessment} object returned by \code{\link[=assess_fair]{assess_fair()}}.}

\item{type}{What to draw. \code{"category"} (default) draws one bar per FAIR
category (Findable, Accessible, Interoperable, Reusable) plus the overall
score; \code{"metric"} draws one bar per individual metric, grouped and colored
by category; \code{"sunburst"} draws a concentric sunburst (an inner ring of the
F/A/I/R categories and an outer ring of the individual metrics, each filled
in proportion to its score) with the overall FAIR percentage in the center.}

\item{colors}{Named character vector of category fill colors, with names
\code{"F"}, \code{"A"}, \code{"I"}, \code{"R"}.}

\item{show_maturity}{Logical; annotate each bar with its maturity level.
Defaults to \code{TRUE} for \code{type = "category"}.}

\item{main}{Title. Defaults to the resolved identifier (or the input id).}

\item{...}{Ignored (for S3 method compatibility).}
}
\value{
\code{x}, invisibly. Called for the side effect of drawing a plot.
}
\description{
Draws a compact, readable scorecard of a \link{fair_assessment}
using base graphics (no extra package dependencies). It is the quickest way to
\emph{see} an assessment: a horizontal progress bar per FAIR category (or per
metric), each annotated with its score and CMMI maturity level. See
\code{vignette("illustrating-fairness")} for worked examples.
}
\examples{
# A stored example assessment (no network needed):
data(fair_example)
plot(fair_example)
plot(fair_example, type = "metric")
plot(fair_example, type = "sunburst")
}
\seealso{
\code{\link[=assess_fair]{assess_fair()}}, \code{\link[=summary.fair_assessment]{summary.fair_assessment()}}, \link{fair_example}
}
